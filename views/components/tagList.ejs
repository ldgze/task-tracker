<h2>Tags:</h2>
<ul>
  <% for (let a of tags) { %>
  <li>
    <a href="<%= '/tags/' + a.tagID + '/edit'  %>"><%= a.name %></a>
    <a href="<%= '/tags/' + a.tagID + '/removeTag/' + a.tagID   %>">❌</a>
  </li>
  <% } %>
</ul>


<%- include("../components/formAddTag.ejs", { task }) %>
